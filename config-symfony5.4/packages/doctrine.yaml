imports:
    - { resource: doctrine_dbal.php }

## default config, not needed now.
## @todo (MH): if you enable ORM, any --conn parameter given to doctrine:migrations:migrate won't work anymore.
##  This is because it will create a default entity manager which overrides any connection passed.
##  So when moving to ORM, we probably need an entity manager for each connection and use --em instead.
#doctrine:
#    orm:
#        auto_generate_proxy_classes: true
#        naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware
#        auto_mapping: true
#        mappings:
#            App:
#                is_bundle: false
#                dir: '%kernel.project_dir%/src/Entity'
#                prefix: 'App\Entity'
#                alias: App

## default config, not needed now
#when@test:
#    doctrine:
#        dbal:
#            # "TEST_TOKEN" is typically set by ParaTest
#            dbname_suffix: '_test%env(default::TEST_TOKEN)%'

## default config, not needed now
#when@prod:
#    doctrine:
#        orm:
#            auto_generate_proxy_classes: false
#            query_cache_driver:
#                type: pool
#                pool: doctrine.system_cache_pool
#            result_cache_driver:
#                type: pool
#                pool: doctrine.result_cache_pool
#
#    framework:
#        cache:
#            pools:
#                doctrine.result_cache_pool:
#                    adapter: cache.app
#                doctrine.system_cache_pool:
#                    adapter: cache.system
