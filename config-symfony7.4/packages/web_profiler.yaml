when@dev:
    web_profiler:
        toolbar: true

    framework:
        # Enabled in Symfony by default:
        # profiler: true
        profiler:
            collect_serializer_data: true

when@test:
    framework:
        profiler:
            collect: false
            collect_serializer_data: true
